name: CI
on:
  pull_request:
  push:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm test
      - run: pnpm check-types
# Deploy from your own fork. Do not commit D1/KV IDs — use GitHub secrets:
#          FOXWATCH_D1_DATABASE_ID: ${{ secrets.FOXWATCH_D1_DATABASE_ID }}
#          FOXWATCH_KV_NAMESPACE_ID: ${{ secrets.FOXWATCH_KV_NAMESPACE_ID }}
#          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
#      - run: pnpm deploy
#        if: github.ref == 'refs/heads/main'
